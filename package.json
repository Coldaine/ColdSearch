{
  "name": "usearch",
  "version": "0.2.0",
  "description": "Unified search CLI - one interface, multiple providers",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "usearch": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "echo \"Run manual tests with: usearch --help\"",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "search",
    "cli",
    "tavily",
    "unified"
  ],
  "author": "Patrick MacLyman",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@bitwarden/sdk-napi": "^1.0.0",
    "@iarna/toml": "^2.2.5",
    "@tavily/core": "^0.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
