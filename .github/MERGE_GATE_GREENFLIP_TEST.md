# merge-gate green-flip test (throwaway)

Temporary file to open a disposable PR that verifies the `merge-gate` check
flips from `action_required` → `success` once the cooldown elapses and a valid
attestation comment is posted. This PR is meant to be **closed, not merged**,
and this file deleted with the branch.
