# PR 5: Agent Run IDs and Trace Correlation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve issue #14 by adding structured run IDs to agent mode and related logs, while strengthening agent/tool-flow observability.

**Architecture:** Generate or accept a run ID at the CLI/agent boundary, store it on the `SearchAgent`/`ResearchContext`, and pass it through backend search/provider-tool calls so usage logs can correlate provider calls to an agent run.

**Tech Stack:** TypeScript, Node.js `crypto`, built-in `node:test`, existing agent and CLI tests.

---

## Scope

Implement:

- Generated run ID for each agent run
- `--run-id <id>`
- `run_id` in agent CLI JSON output
- `run_id` in agent steps
- `run_id` in usage logs created by agent-triggered search
- `run_id` in usage logs created by agent-triggered provider-tool calls
- Richer agent/tool-flow logs with run ID, tool name, selected provider/tool, cache lookup result, timing, and error metadata where available
- Validation for explicit run IDs

Do not implement:

- Global run IDs for every non-agent CLI call
- Distributed trace IDs
- Remote trace storage
- OpenTelemetry
- Secret or prompt transcript logging beyond the existing explicit output contract

## Files

- Modify: `src/agent/agent.ts`
- Modify: `src/agent/context.ts`
- Modify: `src/execution/backend.ts`
- Modify: `src/engine/fanout.ts`
- Modify: `src/logging/usage.ts`
- Modify: `src/types.ts`
- Modify: `src/cli.ts`
- Test: `test/agent-mode.test.mjs`
- Test: `test/agent-payloads.test.mjs`
- Test: `test/cli-integration.test.mjs`

## Run ID Contract

Generated IDs should be log-friendly:

```text
run_20260622T173012Z_7f3a9c
```

Rules:

- Generated IDs start with `run_`.
- Explicit IDs may be provided with `--run-id`.
- Empty or whitespace-only explicit IDs are rejected.
- The same run ID appears in output, steps, and agent-triggered usage entries.
- Non-agent usage log entries remain valid without `run_id`.

## Tasks

- [ ] Add `createRunId()` helper using UTC timestamp plus `crypto.randomBytes(3).toString("hex")`.
- [ ] Add `runId?: string` to agent options.
- [ ] Parse `--run-id` in `src/cli.ts`.
- [ ] Reject empty explicit run IDs.
- [ ] Thread run ID from CLI to `SearchAgent`.
- [ ] Add run ID to `ResearchContext`.
- [ ] Add run ID to agent steps.
- [ ] Add `run_id` to agent CLI output.
- [ ] Add `runId?: string` to backend/fanout options.
- [ ] Pass run ID from agent search tool calls into backend search.
- [ ] Pass run ID from agent provider-tool calls into the provider-tool surface if agent mode uses it.
- [ ] Add `run_id?: string` to `UsageLogEntry`.
- [ ] Write `run_id` only when present.
- [ ] Add safe agent/tool-flow log fields needed to reconstruct provider/tool/cache flow.
- [ ] Confirm no raw API keys or hidden secrets are logged.

## Required Tests

- [ ] `test/agent-mode.test.mjs`: generated agent result includes `run_id`.
- [ ] `test/agent-mode.test.mjs`: explicit run ID is used in output.
- [ ] `test/agent-mode.test.mjs`: empty explicit run ID fails.
- [ ] `test/agent-mode.test.mjs`: every agent step includes the same run ID.
- [ ] `test/cli-integration.test.mjs`: usage entries from agent-triggered search include `run_id`.
- [ ] `test/cli-integration.test.mjs`: usage entries from agent-triggered provider-tool calls include `run_id` when that path exists.
- [ ] `test/cli-integration.test.mjs`: non-agent usage entries remain valid without `run_id`.
- [ ] `test/cli-integration.test.mjs`: agent/tool-flow logs include safe routing/cache/timing metadata.
- [ ] `test/agent-payloads.test.mjs`: existing tool payload parsing remains unchanged.

## Validation

Run before opening the PR:

```bash
npm test
node dist/cli.js --agent --run-id run_manual_check --config <tmp-config> --json "test research goal"
```

Run `npm run test:docs` only if this PR changes provider matrix, registry, config docs, architecture docs, or plan-validator expectations.

What these prove:

- `npm test` proves generated and explicit run IDs flow through agent output, steps, agent-triggered usage logs, and backwards-compatible non-agent logs.
- The manual agent CLI check proves the built command accepts an explicit run ID and returns the public JSON contract. Use a temp config with mocked or intentionally configured provider/LLM settings; do not treat a live LLM answer as the proof.
- `npm run test:docs`, when needed, proves changed docs and registries remain consistent.

Expected:

- Required tests pass.
- Agent output includes `run_id`.
- Usage log entries generated by agent search include `run_id`.
- Non-agent output contracts remain compatible.

## Success Criteria

- Every agent run can be correlated across output, steps, and usage logs.
- Agent-triggered search and provider-tool calls carry the same run ID when they write usage logs.
- Explicit `--run-id` works and invalid values fail early.
- Existing non-agent usage records remain backward compatible.
- Added observability fields are safe by reference and do not log secrets or hidden prompt transcripts.

## PR Review Pause

After implementation:

- [ ] Commit the completed PR 5 scope.
- [ ] Push the branch.
- [ ] Open PR 5 only after validation passes.
- [ ] Wait for CI and advisory reviewers.
- [ ] Read inline review threads, flat comments, bot comments, and check summaries.
- [ ] Address valid findings.
- [ ] Re-run the validation that proves the changed behavior after every follow-up commit. Include `npm test` for runtime changes and `npm run test:docs` only for docs/registry/validator changes.
- [ ] Wait again after every push.
- [ ] Merge PR 5 only after the review pause is complete and the merge protocol is satisfied.
