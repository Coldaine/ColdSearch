# PR 1: Cache A2 and Cache Hygiene Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the local cache operations surface and harden file-backed cache persistence.

**Architecture:** Extend the existing `CacheStore` and keep cache orchestration above `FanoutEngine` in `LocalExecutionBackend`. Do not add SQLite or a daemon in this PR. Keep cache failures non-fatal.

**Tech Stack:** TypeScript, Node.js filesystem APIs, built-in `node:test`, existing JSON file cache layout.

---

## Scope

Implement:

- `coldsearch cache stats`
- `coldsearch cache clear`
- `--freshness <duration>` for `search` and `extract`
- Atomic cache writes
- Restrictive cache directory/file permissions where supported
- Explicit crawl-cache decision

Do not implement:

- `coldsearch batch`
- Daemonized cache state
- Cross-process locking
- Remote cache backend

## Files

- Modify: `src/cache/cache.ts`
- Modify: `src/cache/key.ts`
- Modify: `src/execution/backend.ts`
- Modify: `src/engine/fanout.ts`
- Modify: `src/cli.ts`
- Modify: `src/types.ts`
- Modify: `config.example.toml`
- Modify: `docs/CONFIGURATION.md`
- Modify: `docs/PROGRESS.md`
- Test: `test/cache.test.mjs`
- Test: `test/result-cache.test.mjs`
- Test: `test/cli-integration.test.mjs`

## Crawl-Cache Decision

Default decision: keep crawl uncached in this PR.

Document this reason:

- Crawls can represent broad site snapshots.
- Crawl results are more sensitive to site state and crawl depth.
- Current repeated-work pressure is on search/extract and batch enrichment.
- Crawl cache can be added later with `crawl_ttl` and `{ limit }` in the key if real workloads justify it.

If the user explicitly asks for crawl cache during this PR, implement it in the same cache layer with:

- `crawl_ttl`
- cache key options `{ limit }`
- the same provider-scope bypass behavior as search/extract
- explicit docs and tests

## Tasks

- [ ] Write failing tests for `CacheStore.stats()`.
- [ ] Implement `CacheStore.stats()` returning per-capability counts, expired counts, live counts, and approximate bytes.
- [ ] Write failing tests for `CacheStore.clear()`.
- [ ] Implement `CacheStore.clear(capability?: string)`.
- [ ] Write failing tests for atomic writes.
- [ ] Change `CacheStore.set()` to write to a temporary file and rename it into place.
- [ ] Add restrictive mode options to `mkdirSync` and file writes where supported.
- [ ] Add `freshnessSeconds?: number` to cache-facing options.
- [ ] Parse `--freshness <duration>` using the existing duration parser.
- [ ] Thread freshness into `LocalExecutionBackend.search()` and `extract()`.
- [ ] Add `cache stats` command parsing.
- [ ] Add `cache clear` command parsing.
- [ ] Add optional `--capability search|extract|crawl` for cache commands.
- [ ] Update CLI help with cache commands.
- [ ] Update `docs/CONFIGURATION.md`.
- [ ] Update `config.example.toml`.
- [ ] Update `docs/PROGRESS.md`.

## Required Tests

- [ ] `test/cache.test.mjs`: `stats()` reports live search and extract entries.
- [ ] `test/cache.test.mjs`: `stats()` reports expired entries without returning expired payloads.
- [ ] `test/cache.test.mjs`: `clear("search")` removes search entries and preserves extract entries.
- [ ] `test/cache.test.mjs`: `clear()` removes all entries.
- [ ] `test/cache.test.mjs`: atomic write leaves a readable final entry.
- [ ] `test/result-cache.test.mjs`: freshness shorter than entry age forces a provider call.
- [ ] `test/result-cache.test.mjs`: freshness longer than entry age uses cache.
- [ ] `test/cli-integration.test.mjs`: `cache stats --json` returns valid JSON.
- [ ] `test/cli-integration.test.mjs`: `cache clear --json` reports removed entries.

## Validation

Run before opening the PR:

```bash
npm test
npm run test:docs
node dist/cli.js cache stats --config <temp-config> --json
node dist/cli.js cache clear --config <temp-config> --json
```

Expected:

- All tests pass.
- Cache stats command exits 0.
- Cache clear command exits 0.
- No command prints raw API keys.

## PR Review Pause

After implementation:

- [ ] Commit the completed PR 1 scope.
- [ ] Push the branch.
- [ ] Open PR 1 only after validation passes.
- [ ] Wait for CI, merge-gate, and advisory reviewers.
- [ ] Read inline review threads, flat comments, bot comments, and check summaries.
- [ ] Address valid findings.
- [ ] Re-run `npm test` and `npm run test:docs` after every follow-up commit.
- [ ] Wait again after every push.
- [ ] Do not start PR 2 until PR 1 is merged unless the user explicitly authorizes parallel work.

